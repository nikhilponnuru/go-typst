[package]
name = "typst-ffi"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]

[dependencies]
typst = "0.14"
typst-pdf = "0.14"
typst-assets = { version = "0.14", features = ["fonts"] }
comemo = "0.4"
chrono = "0.4"

[profile.release]
opt-level = 2
lto = true
